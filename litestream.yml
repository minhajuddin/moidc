dbs:
  - path: /data/moidc.db
    replicas:
      - type: s3
        bucket: ${LITESTREAM_S3_BUCKET}
        path: moidc.db
        endpoint: ${LITESTREAM_S3_ENDPOINT}
        region: ${LITESTREAM_S3_REGION}
